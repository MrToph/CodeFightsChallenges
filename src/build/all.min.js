/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(1);


/***/ },
/* 1 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';
	
	var _ArrayMultiDim = __webpack_require__(2);
	
	var _ArrayDynamic = __webpack_require__(3);
	
	// import MathContainer from './MathContainer'
	// import Stepper from './Stepper'
	
	// let tests = new ArrayMultiDimTests()
	// tests.run()
	var tests = new _ArrayDynamic.ArrayDynamicTests();
	tests.run();

/***/ },
/* 2 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();
	
	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
	
	var ArrayMultiDim = function () {
	  function ArrayMultiDim(sizes) {
	    _classCallCheck(this, ArrayMultiDim);
	
	    if (!Array.isArray(sizes) || sizes.length < 1) throw new Error('not an array or array does not have any dimensions');
	    var size = sizes.reduce(function (acc, x) {
	      return acc * x;
	    }, 1); // multiply all dimension sizes
	    this.sizes = sizes;
	    this.size = size;
	    this.arr = new Array(size);
	    this.precomputeAccessValues();
	  }
	
	  _createClass(ArrayMultiDim, [{
	    key: 'get',
	    value: function get(indices) {
	      return this.arr[this.layoutMap(indices)];
	    }
	  }, {
	    key: 'set',
	    value: function set(indices, value) {
	      this.arr[this.layoutMap(indices)] = value;
	    }
	  }, {
	    key: 'precomputeAccessValues',
	    value: function precomputeAccessValues() {
	      this.products = new Array(this.sizes.length).fill(1); // initialize with 1 everywhere
	      // last index does not need to be multiplied
	      for (var i = this.products.length - 2; i >= 0; i--) {
	        this.products[i] *= this.sizes[i + 1] * this.products[i + 1];
	      }
	    }
	
	    // row major, i.e. last dimension contiguous
	
	  }, {
	    key: 'layoutMap',
	    value: function layoutMap(indices) {
	      var _this = this;
	
	      if (indices.length !== this.sizes.length) throw new Error('indices is of wrong length');
	      if (!indices.reduce(function (acc, val, index) {
	        return acc && val >= 0 && val < _this.sizes[index];
	      }, true)) throw new Error('some index is out of bounds');
	      return indices.reduce(function (acc, val, index) {
	        return acc + val * _this.products[index];
	      }, 0);
	    }
	  }]);
	
	  return ArrayMultiDim;
	}();
	
	exports.default = ArrayMultiDim;
	;
	
	var ArrayMultiDimTests = exports.ArrayMultiDimTests = function () {
	  function ArrayMultiDimTests() {
	    _classCallCheck(this, ArrayMultiDimTests);
	  }
	
	  _createClass(ArrayMultiDimTests, [{
	    key: 'run',
	    value: function run() {
	      try {
	        var _arr = new ArrayMultiDim([]);
	        console.log('Failed');
	      } catch (e) {
	        console.log('Passed. ' + e);
	      }
	
	      try {
	        var _arr2 = new ArrayMultiDim('hello');
	        console.log('Failed');
	      } catch (e) {
	        console.log('Passed. ' + e);
	      }
	
	      try {
	        var _arr3 = new ArrayMultiDim([5]);
	        _arr3.get([5]);
	        console.log('Failed');
	      } catch (e) {
	        console.log('Passed. ' + e);
	      }
	
	      try {
	        var _arr4 = new ArrayMultiDim([3, 5, 4]);
	        _arr4.get([5]);
	        console.log('Failed');
	      } catch (e) {
	        console.log('Passed. ' + e);
	      }
	
	      var arr = new ArrayMultiDim([3, 5, 4]);
	      var c = 0;
	      for (var i = 0; i < arr.sizes[0]; i++) {
	        for (var j = 0; j < arr.sizes[1]; j++) {
	          for (var k = 0; k < arr.sizes[2]; k++) {
	            arr.set([i, j, k], c++);
	          }
	        }
	      }
	      console.log(arr.products);
	      console.log(arr.get([2, 2, 2]));
	    }
	  }]);
	
	  return ArrayMultiDimTests;
	}();
	
	;

/***/ },
/* 3 */
/***/ function(module, exports) {

	'use strict';
	
	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	
	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();
	
	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }
	
	var ArrayDynamic = function () {
	  function ArrayDynamic(capacity) {
	    _classCallCheck(this, ArrayDynamic);
	
	    this.top = -1;
	    this.growFactor = 2;
	    this.shrinkFactor = 0.25;
	    this.minCapacity = 4; // should be divisible by 1/shrinkfactor for our purposes
	    this.capacity = Math.min(this.minCapacity, capacity);
	    this.arr = new Array(this.capacity);
	  }
	
	  _createClass(ArrayDynamic, [{
	    key: 'get',
	    value: function get(index) {
	      if (index < 0 || index >= this.capacity) throw Error('out of bounds');
	      return this.arr[index];
	    }
	  }, {
	    key: 'push',
	    value: function push(value) {
	      this.insertAt(this.top + 1, value);
	    }
	  }, {
	    key: 'insertAt',
	    value: function insertAt(index, value) {
	      var oldCapacity = this.capacity;
	      // make index fit inside our array
	      while (index > this.capacity - 1) {
	        this.capacity *= this.growFactor;
	      }
	      // set new top eventually
	      var oldTop = this.top;
	      this.top = Math.max(this.top, index);
	      // have to grow array?
	      if (this.top >= this.capacity / this.growFactor) {
	        this.capacity *= this.growFactor;
	      }
	      // copy stuff over
	      var newArr = void 0;
	      if (oldCapacity < this.capacity) {
	        // capacity changed => relocate
	        newArr = new Array(this.capacity); // have to create a new one
	        // copy from 0 up to insert index or to the old top (if insert index > oldTop)
	        for (var i = 0; i < Math.min(index, oldTop + 1); i++) {
	          newArr[i] = this.arr[i];
	        }
	      } else {
	        newArr = this.arr; // do it in place
	      }
	      // only have to shift interval [index, oldTop] one to the right
	      // (only happens if inserting before oldTop, i.e. index < oldTop)
	      for (var _i = oldTop + 1; _i > index; _i--) {
	        newArr[_i] = this.arr[_i - 1]; // right shift
	      }
	      newArr[index] = value;
	      this.arr = newArr;
	    }
	  }, {
	    key: 'remove',
	    value: function remove(index) {
	      if (index < 0 || index > this.top) throw Error('out of bounds');
	      var newTop = this.top; // indicates the new top value AFTER shifting to the left (so at the end of the function)
	      if (index < this.top) newTop--; // newTop will be the old one just shifted one to the left (because the remove happens to the left of it)
	      else {
	          // otherwise have to find newTop by looking for largest index that is not undefined
	          newTop = this.top - 1; // index > this.top, index < this.top both doesn't hold => index = top, start one from the left of it to look for non-undefined values
	          while (newTop >= 0) {
	            if (this.arr[newTop] !== undefined) break;
	            newTop--;
	          }
	        }
	      // given newTop we can now check for shrinking the array
	      var oldCapacity = this.capacity;
	      if (newTop < this.minCapacity * this.shrinkFactor) this.capacity = this.minCapacity;
	      while (newTop < this.capacity * this.shrinkFactor && this.capacity > this.minCapacity) {
	        this.capacity *= this.shrinkFactor; // no flooring needed as minCapacity is divisible by 4 (1/shrinkfactor)
	      }
	      this.capacity = Math.max(this.capacity, this.minCapacity);
	      // copy stuff over, eventually create new array
	      var newArr = void 0;
	      if (oldCapacity > this.capacity) {
	        // capacity changed => relocate
	        newArr = new Array(this.capacity); // have to create a new one
	        // copy from 0 up to remove index (=> newTop = top - 1) or up to the new top (if remove index > newTop, i.e. index == top got removed)
	        for (var i = 0; i < Math.min(index, newTop + 1); i++) {
	          newArr[i] = this.arr[i];
	        }
	      } else {
	        newArr = this.arr; // do it in place
	      }
	      // only have to shift interval [index+1, top] one to the left
	      // (only happens if removing left of top, i.e. index < newTop)
	      for (var _i2 = index; _i2 < newTop + 1; _i2++) {
	        newArr[_i2] = this.arr[_i2 + 1]; // left shift
	      }
	      delete this.arr[this.top]; // when we remove, top always decreases, we need to clear this one (again this is only needed when index == top got removed)
	      this.top = newTop;
	      this.arr = newArr;
	    }
	  }]);
	
	  return ArrayDynamic;
	}();
	
	exports.default = ArrayDynamic;
	;
	
	var ArrayDynamicTests = exports.ArrayDynamicTests = function () {
	  function ArrayDynamicTests() {
	    _classCallCheck(this, ArrayDynamicTests);
	  }
	
	  _createClass(ArrayDynamicTests, [{
	    key: 'run',
	    value: function run() {
	      var arr = new ArrayDynamic(5);
	      this.log(arr);
	      for (var i = 1; i < 15; i++) {
	        arr.push(i);
	        this.log(arr);
	      }
	      arr.insertAt(0, 0);
	      this.log(arr);
	      arr.insertAt(49, 50);
	      this.log(arr);
	      arr.remove(49);
	      this.log(arr);
	      arr.remove(0);
	      this.log(arr);
	      for (var _i3 = 0; _i3 < 13; _i3++) {
	        arr.remove(0);
	        this.log(arr);
	      }
	      arr.insertAt(0, 0);
	      this.log(arr);
	    }
	  }, {
	    key: 'log',
	    value: function log(obj) {
	      console.log(obj.arr.slice(), obj.capacity); // create copy
	    }
	  }]);
	
	  return ArrayDynamicTests;
	}();
	
	;

/***/ }
/******/ ]);
//# sourceMappingURL=all.min.js.map